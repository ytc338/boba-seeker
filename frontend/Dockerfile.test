# Dockerfile for running frontend tests
FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install all dependencies (including dev dependencies)
# Using npm install instead of npm ci to handle new deps
RUN npm install

# Copy source code
COPY . .

# Default command runs tests
CMD ["npm", "test", "--", "--run"]


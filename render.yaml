databases:
  - name: boba-seeker-db
    plan: free
    databaseName: boba_seeker

services:
  - type: web
    name: boba-seeker-backend
    runtime: python
    plan: free
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: boba-seeker-db
          property: connectionString
      - key: FRONTEND_URL
        sync: false  # Set to frontend URL after deploy (e.g., https://boba-seeker-frontend.onrender.com)
      - key: GOOGLE_PLACES_API_KEY
        sync: false  # Optional: Set in Render dashboard
    healthCheckPath: /health

  - type: web
    name: boba-seeker-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm ci && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        sync: false  # Set to backend URL after deploy (e.g., https://boba-seeker-backend.onrender.com)
      - key: VITE_MAPBOX_TOKEN
        sync: false  # Required: Set in Render dashboard
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

